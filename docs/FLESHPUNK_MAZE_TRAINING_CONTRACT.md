# Fleshpunk Maze Training Contract

Confirmed commission: convert Infinite Brutality toward a functional `fleshpunk_maze` family by training ink-line assets that serve the `lift_court_hybrid` maze structure.

## Prompt Contract Ledger

- Current command: implement the confirmed Fleshpunk Maze Asset-Training Plan.
- Binding repeated prompts: hard mutation gate; useful objects and creatures; no generic flesh blobs; exploded machinery; ink lineart only; pipes, valves, tendons, connective tissue.
- Forbidden stale premises: generic brutalist box work, portrait tracing, decorative flesh, poisoned Venice runtime, hidden inference, local screenshots as visual acceptance.
- Required artifacts: strict asset catalog and compiler, deterministic canary plates, Drive provenance, clean Cauldron adapter, checkpoint receipts, proof-district bindings.
- Accepted evidence: Boxcraft and RegNet are internal gates; cloud/Sense pixels and explicit user verdict own acceptance.
- False green: logs, source strings, missing canaries, leaked validation, or an average score overriding one failed family.

## Causal Pre-Mortem

`Drive receipt -> grouped lineart dataset -> ASSET REQUEST 1 -> clean trainer -> checkpoint -> fixed-seed canaries -> anatomy ablation -> RegNet -> Boxcraft -> lift_court_hybrid -> cloud/Sense`. Each edge fails closed, names its missing input, preserves the parent checkpoint, and emits a receipt. Props retain host, base, supports, working body, termination, service route, silhouette, collision, and clearance.

## State Boundary

The Drive source corpus is metadata-verified but its bytes are not yet imported. The historical LoRA exists on THECAULDRON, but remains hash-pending until copied as the one permitted quarantine extraction. No runtime activation or human acceptance is implied by this contract.

## 2026-09-05 implementation retrospective

- Result: `FRONTIER_EXPANSION`. The source intake, deterministic line projection, Cauldron scheduler gate, RegNet review, and Boxcraft mutation decision are implemented and callable; no accepted model checkpoint or runtime asset is claimed.
- Soundness: 20 Drive PNGs and 20 captions match provider byte counts and decode; 20 legacy manifest digests are preserved as stale mismatches. Eight Drive plates pass deterministic density/shape gates, twelve are quarantined, and one authored P0 valve plate supplements the projection. Local and Cauldron decoded-pixel aggregate hashes match.
- Negative controls: dense/photo/blob plates are rejected; a RegNet reject forces `DENY_MUTATION`; unsigned user pixels stop the scheduled runner at `OBSERVE` before dependency download or training.
- Weakest clauses: `drw_fleshpunk_maze_line_v2.safetensors` has not been trained; no real Comfy recreation/mutation or cloud/Sense runtime acceptance exists yet. These are deliberately blocked by the unsigned user pixel gate.
- Waste: the first Drive download path mistook app-scoped handles for shell URLs; the first PTY transport hit canonical line limits; one shared-storage patch attempt was rejected; the first line-stat oracle used floating subtraction; one validation command used the wrong working directory. None was promoted as success.
- Improvement: start future Drive binary intake through the existing folder-ID-bound rclone or noncanonical no-echo transport, compare canonical decoded pixels across Pillow platforms, and run path-sensitive checks from project root.
- Exact local Codex tokens: `UNKNOWN`; par history is not updated.
